{
	"$schema": "https://turborepo.dev/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"inputs": ["$TURBO_DEFAULT$", "prisma/**"],
			"outputs": ["src/generated/**"],
			"passThroughEnv": ["DATABASE_URL"]
		},
		"dev:prepare": {
			"cache": false,
			"inputs": [
				"prisma/schema.prisma",
				"prisma/migrations/**",
				"prisma.config.ts",
				"scripts/prepare-dev.ts",
				"scripts/require-local-db.ts"
			],
			"outputs": ["src/generated/**"],
			"passThroughEnv": ["ALLOW_REMOTE_DB", "DATABASE_URL"]
		},
		"db:generate": {
			"inputs": ["$TURBO_DEFAULT$", "prisma/**"],
			"outputs": ["src/generated/**"],
			"passThroughEnv": ["DATABASE_URL"]
		},
		"db:deploy": {
			"cache": false,
			"passThroughEnv": ["DATABASE_URL"]
		},
		"db:migrate": {
			"cache": false,
			"interactive": true,
			"passThroughEnv": ["DATABASE_URL"]
		},
		"db:push": {
			"cache": false,
			"interactive": true,
			"passThroughEnv": ["DATABASE_URL"]
		},
		"db:reset": {
			"cache": false,
			"interactive": true,
			"passThroughEnv": ["DATABASE_URL"]
		},
		"db:seed": {
			"cache": false,
			"passThroughEnv": [
				"STORAGE_ENDPOINT",
				"STORAGE_REGION",
				"STORAGE_BUCKET",
				"STORAGE_ACCESS_KEY_ID",
				"STORAGE_SECRET_ACCESS_KEY",
				"STORAGE_PUBLIC_URL",
				"DATABASE_URL"
			]
		},
		"db:studio": {
			"cache": false,
			"persistent": true,
			"passThroughEnv": ["DATABASE_URL"]
		},
		"check-types": {
			"dependsOn": ["$TURBO_EXTENDS$", "build"],
			"passThroughEnv": ["DATABASE_URL"]
		}
	}
}
